<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Matches the CSS bg token in src/index.css. Used as the native window
         background so there's no gap where the OS default (white) could
         show through between the splash screen dismissing and the WebView's
         own CSS painting. -->
    <color name="windowBackground">#06070a</color>
</resources>
